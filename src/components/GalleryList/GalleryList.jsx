
function GalleryList (props) {

    return (
        <>
        <ul>
            {gallery.map(image =>(
                <li key={image.id}>


                </li>

            )
            }
        </ul>
        </>
    )
}